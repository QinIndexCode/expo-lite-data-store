export const RATE_LIMIT = {
  DEFAULT_RATE: 100,
  DEFAULT_CAPACITY: 200,
  DEFAULT_RESET_TIME: 1000,
} as const;

export const CACHE = {
  DEFAULT_MAX_SIZE: 1000,
  DEFAULT_EXPIRY: 3600000,
  CLEANUP_INTERVAL: 300000,
  AVALANCHE_PROTECTION_RANGE: [0, 300000] as [number, number],
  MEMORY_THRESHOLD: 0.8,
  PENETRATION_PROTECTION_TTL: 60000,
} as const;

export const FILE_OPERATION = {
  DEFAULT_CHUNK_SIZE: 5 * 1024 * 1024,
  MAX_TABLE_NAME_LENGTH: 100,
  OPERATION_TIMEOUT: 10000,
  RETRY_DELAY: 100,
} as const;

export const QUERY = {
  DEFAULT_PAGE_SIZE: 100,
  MAX_PAGE_SIZE: 1000,
  COUNTING_SORT_THRESHOLD: 100,
  MERGE_SORT_THRESHOLD: 10000,
} as const;

export const MONITORING = {
  DEFAULT_INTERVAL: 60000,
  MAX_HISTORY_RECORDS: 100,
} as const;

export const REGEX = {
  TABLE_NAME: /^[a-zA-Z][a-zA-Z0-9_]*$/,
  VALID_CHARS: /^[a-zA-Z0-9_\-]+$/,
} as const;
